// console.log('It\'s Works!')
// CASE SWITCH
// Make many if more Simple
var inspect = 1,
	result = '';

switch (inspect) {
	case 0:
		result = "zero";		
		break;
	case 1:
		result = "one";
		break;
	default:
		result: "unknown";
}
// Many way to convert String to Number
var year = "2018";
parseInt(year);
+year;
Number(year);
